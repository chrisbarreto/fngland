---
import Section from "./Section.astro";

const title = "Activá tu <span class='light-text'>Plan Premium</span> hoy mismo";

---

<div class="cta-container">
  <div class="cta-bg">
    <img src="/cta-bg.webp" alt="Auto recibiendo lavado premium en Fórmula NG" class="cta-bg-img" />
    <div class="cta-bg-overlay"></div>
  </div>
  <Section title={title}>
    <div class="cta-buttons">
      <button
        type="button"
        class="cta-btn"
        id="cta-registro-btn"
      >
        QUIERO ACTIVAR MI PLAN
      </button>
    </div>
    <p class="cta-disclaimer">
      Permanencia mínima de 12 meses. Débito automático obligatorio. Aplican <a href="/terminos-y-condiciones" style="color:inherit;text-decoration:underline;">términos y condiciones</a>.
    </p>
  </Section>
</div>

<style>
  .cta-container {
    padding: 1rem;
    position: relative;
    overflow: hidden;
  }
  .cta-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
  }
  .cta-bg-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .cta-bg-overlay {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.55);
    pointer-events: none;
  }
  .cta-container :global(.section) {
    position: relative;
    z-index: 1;
  }

  .cta-btn {
    background: linear-gradient(135deg, #f92606, #ff6b3d);
    color: white;
    padding: 1rem 2.5rem;
    border-radius: 8px;
    font-weight: 600;
    font-size: 1rem;
    text-decoration: none;
    display: inline-block;
    transition: all 0.3s ease;
    box-shadow: 0 4px 20px rgba(249, 38, 6, 0.3);
    border: 2px solid transparent;
    cursor: pointer;
  }

  .cta-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(249, 38, 6, 0.5);
    border-color: rgba(249, 38, 6, 0.5);
  }

  .cta-buttons {
    display: flex;
    justify-content: center;
  }

  .cta-disclaimer {
    margin: 0.9rem 0 0;
    text-align: center;
    color: var(--sl-color-gray-3);
    font-size: 0.76rem;
    line-height: 1.35;
    letter-spacing: 0.01em;
  }
</style>

<script>
  const ctaBtn = document.getElementById("cta-registro-btn");
  ctaBtn?.addEventListener("click", () => {
    if (typeof (window as any).__openRegistroModal === "function") {
      (window as any).__openRegistroModal();
    }
  });
</script>
